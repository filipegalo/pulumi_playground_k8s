{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "paas-service.name" . }}
  labels:
    {{- include "paas-service.labels" . | nindent 4 }}
spec:
  {{- toYaml .Values.networkPolicy.spec | nindent 2 }}
{{- end }}
